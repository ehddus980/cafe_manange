# 데이터베이스 설정
# DATABASE_TYPE=sqlite  # 개발용 (기본값)
DATABASE_TYPE=postgresql  # 운영용

# PostgreSQL 설정 (DATABASE_TYPE=postgresql일 때 사용)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cafe_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# 보안 설정
SECRET_KEY=your-secret-key-change-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=secure-password-change-in-production 